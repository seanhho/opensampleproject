var should = require('should');
var firebase = require('../app/modules/firebase')

describe('firebase module', function(){
	describe('#pingApp', function(){
		it('should post to firebase', function(callback){
			// firebase.pingApp('from', 'to', {what: 'what'}, function(err, n){
			 	callback()
			// })
		})
	})
})